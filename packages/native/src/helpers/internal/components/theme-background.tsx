/**
 * Internal re-export of the public ThemeBackground component so component
 * wrappers can keep importing it from `helpers/internal/components`.
 */
export {
  ThemeBackground,
  useHasDefaultThemeBackground,
} from '../../../components/theme-background';
export type { ThemeBackgroundProps } from '../../../components/theme-background';
